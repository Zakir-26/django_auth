# Django Authentication

This project includes a secure user authentication system built using Django’s built-in authentication framework. It supports user registration, login and logout.

The system ensures that only authenticated users can access protected pages like the dashboard, while unauthenticated users are redirected to the login page.

Key highlights:
- User signup with form validation
- Secure login and logout functionality
- Session-based authentication
- Protected routes using authentication checks
